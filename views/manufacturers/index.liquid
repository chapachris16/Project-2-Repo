{% layout "layout.liquid" %} {% block content %}
<div>
    <a href="/manufacturers/new"><button>Enter New Manufacturer</button></a>
  
    {% for manufacturer in manufacturers %}
    <article>
      <a href="/manufacturers/{{ manufacturer._id }}">
        <h2>
          {{manufacturer.name}}
        </h2>
      </a>
    </article>
    {% endfor %}
</div>
{%endblock%}