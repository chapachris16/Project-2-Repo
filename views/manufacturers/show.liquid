{% layout "layout.liquid" %} {% block content %}
    <div>
        <article>
        <h2>
            {{ manufacturer.name }}
        </h2>
        {%for models in manufacturer %}
        <a href="/cars/{{manufacturer.cars[forloop.index0]._id}}"><h3>{{ manufacturer.cars[forloop.index0].name}}</h3>
        <img src={{manufacturer.cars[forloop.index0].img}}>
        </a>
        {%endfor%}
        <a href="/manufacturers/{{ manufacturer._id }}/edit"><button>Edit</button></a>
        <form action="/manufacturers/{{ manufacturer._id }}?_method=DELETE" method="POST">
            <input type="submit" value="Delete" />
        </form>
        <a href="/manufacturers/"><button>Back to Manufacturers Page</button></a>
        </article>
    </div>
{% endblock %}